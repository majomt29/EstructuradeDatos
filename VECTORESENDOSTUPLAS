# Escribir un programa que almacene los vectores (1,2,3) y (-1,0,2) en dos tuplas y muestre por pantalla su producto escalar

X = (1, 2, 3)
Y = (-1, 0, 2)
producto = 0
for i in range(len(X)):
    producto += X[i]*Y[i]
print("El producto de los vectores" + str(X) + " y " + str(Y) + " 
